@page
@model LoginModel

@{
    ViewData["Title"] = "Login";
}



<div class="d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="col-lg-5 col-md-7 col-12">
        <div class="card auth-card shadow-lg border-0">
            <div class="card-header text-center">
                <h3 class="fw-bold mb-1">Welcome Back!</h3>
                <p class="mb-0 text-white-50">Log in to access your AI-powered dashboard.</p>
            </div>
            <div class="card-body px-4 py-5">
                <form id="account" method="post" autocomplete="off">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

                    <div class="mb-4 position-relative form-floating">
                        <input asp-for="Input.Email" class="form-control ps-5" autocomplete="username" aria-required="true" placeholder="Email" />
                        <label asp-for="Input.Email"><i class="bi bi-envelope form-icon"></i> Email</label>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>

                    <div class="mb-4 position-relative form-floating">
                        <input asp-for="Input.Password" class="form-control ps-5" autocomplete="current-password" aria-required="true" placeholder="Password" />
                        <label asp-for="Input.Password"><i class="bi bi-lock form-icon"></i> Password</label>
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>

                    <div class="form-check mb-4">
                        <input class="form-check-input" asp-for="Input.RememberMe" id="rememberMeCheck" />
                        <label class="form-check-label" for="rememberMeCheck">
                            Remember me
                        </label>
                    </div>

                    <button id="login-submit" type="submit" class="btn btn-primary w-100 fw-bold py-2 shadow">Log In</button>

                    <div class="text-center mt-4 fs-6">
                        <a asp-page="./ForgotPassword">Forgot your password?</a> &nbsp;|&nbsp;
                        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl">Register</a>
                    </div>
                </form>
            </div>
            @if ((Model.ExternalLogins?.Count ?? 0) > 0)
            {
                <div class="card-footer bg-light text-center border-0 pt-4 pb-3">
                    <p class="mb-2 text-secondary">Or log in using:</p>
                    <form asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="d-flex justify-content-center gap-2">
                        @foreach (var provider in Model.ExternalLogins!)
                        {
                            <button type="submit" class="btn btn-outline-secondary" name="provider" value="@provider.Name">
                                <i class="bi bi-box-arrow-in-right me-1"></i> @provider.DisplayName
                            </button>
                        }
                    </form>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
